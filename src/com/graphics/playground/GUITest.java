package com.graphics.playground;

import javafx.application.Application;
import javafx.stage.Stage;

public class GUITest extends Application {

    public static void main(String[] args) {
//      A static method which creates an instance of the app, then calls init(), start(), and stop() when code
//      execution is finished.
        launch(args);
    }

//    The start() method is derived from the abstract class Application. This is the entry point where the JavaFX code
//    will be written.
    @Override
    public void start(Stage primaryStage)
    {

    }

//    The init() method is derived from the abstract class Application.
    @Override
    public void init()
    {

    }

//    The stop() method is derived from the abstract class Application.
    @Override
    public void stop()
    {

    }
}
